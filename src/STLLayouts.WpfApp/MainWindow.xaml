<Window x:Class="STLLayouts.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:STLLayouts.WpfApp"
        xmlns:views="clr-namespace:STLLayouts.WpfApp.Views"
        xmlns:viewmodels="clr-namespace:STLLayouts.WpfApp.ViewModels"
        mc:Ignorable="d"
        Title="STL Layouts - Document Generation System" 
    Height="700" Width="1200"
    Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#ecf0f1"/>
            <Setter Property="Foreground" Value="#2c3e50"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock Text="STL Layouts - Document Generation System" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Job Selection &amp; Document Generation">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search Panel -->
                    <GroupBox Header="Search Jobs" Grid.Row="0" Margin="5">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Job Number:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Column="1" Text="{Binding JobNumber, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Grid.Column="2" Text="Customer:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Column="3" Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"/>

                                <Button Grid.Column="4" Content="Search" Command="{Binding SearchCommand}"/>
                            </Grid>
                        </StackPanel>
                    </GroupBox>

                    <!-- Jobs Grid -->
                    <GroupBox Header="Jobs" Grid.Row="1" Margin="5">
                        <DataGrid ItemsSource="{Binding Jobs}" 
                                  SelectedItem="{Binding SelectedJob}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Job Number" Binding="{Binding JobNumber}" Width="150"/>
                                <DataGridTextColumn Header="Product" Binding="{Binding ProductDescription}" Width="*"/>
                                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="200"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding JobStatus}" Width="100"/>
                                <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>

                    <!-- Actions Panel -->
                    <GroupBox Header="Actions" Grid.Row="2" Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Generate Document" 
                                    Command="{Binding GenerateDocumentCommand}"/>
                            <TextBlock Text="{Binding StatusMessage}" 
                                       VerticalAlignment="Center" 
                                       Margin="20,0,0,0"
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Suggested Templates -->
                    <GroupBox Header="Suggested Templates" Grid.Row="3" Margin="5">
                        <DataGrid ItemsSource="{Binding SuggestedTemplates}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Template Name" Binding="{Binding TemplateName}" Width="*"/>
                                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                                <DataGridTextColumn Header="Version" Binding="{Binding CurrentVersion}" Width="80"/>
                                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>
                </Grid>
            </TabItem>

            <TabItem x:Name="TemplateTab" Header="Templates">
                <!-- Content set in code-behind -->
            </TabItem>

            <TabItem x:Name="VariableMappingsTab" Header="Variable Mappings">
                <!-- Content set in code-behind -->
            </TabItem>

            <TabItem x:Name="DocumentPreviewTab" Header="Document Preview">
                <!-- Content set in code-behind -->
            </TabItem>

            <TabItem x:Name="SettingsTab" Header="Settings">
                <!-- Content set in code-behind -->
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
