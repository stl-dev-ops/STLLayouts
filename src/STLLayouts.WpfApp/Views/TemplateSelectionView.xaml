<UserControl x:Class="STLLayouts.WpfApp.Views.TemplateSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White"
             Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Template Selection" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8" Height="32" ToolTip="Search templates by name or description..."/>

            <ComboBox Grid.Column="1" ItemsSource="{Binding Categories}" 
                      SelectedItem="{Binding SelectedCategory}" Margin="10,0,0,0" Height="32"/>

            <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshCommand}" 
                    Width="80" Margin="10,0,0,0" Height="32"/>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,0,0,10"/>

        <!-- Templates List -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding TemplateName}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                <DataGridTextColumn Header="Format" Binding="{Binding FileExtension}" Width="100"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Indicator -->
        <TextBlock Grid.Row="3" Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Upload/Register Template" Command="{Binding UploadTemplateCommand}"
                    Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Manage Rules" Command="{Binding ManageRulesCommand}"
                Padding="15,8" Margin="0,0,10,0"/>
                <Button Content="Load Templates" Command="{Binding LoadTemplatesCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Visibility="Collapsed"/>
            <Button Content="Close" Width="80" Padding="15,8"/>
        </StackPanel>
    </Grid>
</UserControl>
