<UserControl x:Class="STLLayouts.WpfApp.Views.RuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Rule Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding RuleName}" Width="200"/>
                <DataGridTextColumn Header="Condition" Binding="{Binding Condition}" Width="*"/>
                <DataGridTextColumn Header="TemplateId" Binding="{Binding TemplateId}" Width="250"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="100"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Add Rule" Command="{Binding AddRuleCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Edit Rule" Command="{Binding EditRuleCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Delete Rule" Command="{Binding DeleteRuleCommand}" Padding="15,8"/>
        </StackPanel>
    </Grid>
</UserControl>
