<UserControl x:Class="STLLayouts.WpfApp.Views.RuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">
    <Grid x:Name="RootGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Rule Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <telerik:RadGridView Grid.Row="1" ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule}"
                             AutoGenerateColumns="False" IsReadOnly="True" ShowGroupPanel="False">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Name" DataMemberBinding="{Binding RuleName}" Width="200"/>
                <telerik:GridViewDataColumn Header="Condition" DataMemberBinding="{Binding Condition}" Width="*"/>
                <telerik:GridViewDataColumn Header="TemplateId" DataMemberBinding="{Binding TemplateId}" Width="250"/>
                <telerik:GridViewDataColumn Header="Priority" DataMemberBinding="{Binding Priority}" Width="100"/>
                <telerik:GridViewCheckBoxColumn Header="Active" DataMemberBinding="{Binding IsActive}" Width="80"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <telerik:RadButton Content="Refresh" Command="{Binding RefreshCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <telerik:RadButton Content="Add Rule" Command="{Binding AddRuleCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <telerik:RadButton Content="Edit Rule" Command="{Binding EditRuleCommand}" Padding="15,8" Margin="0,0,10,0"/>
            <telerik:RadButton Content="Delete Rule" Command="{Binding DeleteRuleCommand}" Padding="15,8"/>
        </StackPanel>
    </Grid>
</UserControl>
