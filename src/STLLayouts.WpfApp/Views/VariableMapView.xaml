<UserControl x:Class="STLLayouts.WpfApp.Views.VariableMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White"
             Loaded="UserControl_Loaded"
             IsVisibleChanged="UserControl_IsVisibleChanged">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Variable Mappings" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"
                      SelectedIndex="0"
                      Height="32" Width="200"/>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,0,0,10"/>

        <!-- Mappings List -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding VariableMappings}" SelectedItem="{Binding SelectedMapping}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Variable Name" Binding="{Binding VariableName}" Width="150"/>
                <DataGridTextColumn Header="Database Field" Binding="{Binding DatabaseField}" Width="150"/>
                <DataGridTextColumn Header="Data Type" Binding="{Binding DataType}" Width="100"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                <DataGridCheckBoxColumn Header="Required" Binding="{Binding IsRequired}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Create Variable" Command="{Binding CreateVariableCommand}" 
                    Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Load Mappings" Command="{Binding LoadMappingsCommand}" 
                    Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Save" Command="{Binding SaveMappingCommand}" 
                    Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Delete" Command="{Binding DeleteMappingCommand}" 
                    Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Close" Width="80" Padding="15,8"/>
        </StackPanel>
    </Grid>
</UserControl>
